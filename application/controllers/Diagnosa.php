<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Diagnosa extends MY_Controller {

    // protected $access = array('Admin', 'Editor','Author');
    
    function __construct()
    {
        parent::__construct();
        $this->load->model('Diagnosa_model');
        $this->load->library('form_validation');
    }

    public function index()
    {
        $q = urldecode($this->input->get('q', TRUE));
        $start = intval($this->input->get('start'));
        
        if ($q <> '') {
            $config['base_url'] = base_url() . 'diagnosa/index.html?q=' . urlencode($q);
            $config['first_url'] = base_url() . 'diagnosa/index.html?q=' . urlencode($q);
        } else {
            $config['base_url'] = base_url() . 'diagnosa/index.html';
            $config['first_url'] = base_url() . 'diagnosa/index.html';
        }

        $config['per_page'] = 10;
        $config['page_query_string'] = TRUE;
        $config['total_rows'] = $this->Diagnosa_model->total_rows($q);
        $diagnosa = $this->Diagnosa_model->get_limit_data($config['per_page'], $start, $q);

        $this->load->library('pagination');
        $this->pagination->initialize($config);

        $data = array(
            'diagnosa_data' => $diagnosa,
            'q' => $q,
            'pagination' => $this->pagination->create_links(),
            'total_rows' => $config['total_rows'],
            'start' => $start,
        );
        $this->load->view('header');
        $this->load->view('diagnosa_list', $data);
        $this->load->view('footer');
    }

    public function read($id) 
    {
        $row = $this->Diagnosa_model->get_by_id($id);
        if ($row) {
            $data = array(
		'id' => $row->id,
		'diagnosa' => $row->diagnosa,
	    );
            $this->load->view('header');
            $this->load->view('diagnosa_read', $data);
            $this->load->view('footer');
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('diagnosa'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('diagnosa/create_action'),
	    'id' => set_value('id'),
	    'diagnosa' => set_value('diagnosa'),
	);

        $this->load->view('header');
        $this->load->view('diagnosa_form', $data);
        $this->load->view('footer');
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
		'diagnosa' => $this->input->post('diagnosa',TRUE),
	    );

            $this->Diagnosa_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('diagnosa'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Diagnosa_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('diagnosa/update_action'),
		'id' => set_value('id', $row->id),
		'diagnosa' => set_value('diagnosa', $row->diagnosa),
	    );
            $this->load->view('header');
            $this->load->view('diagnosa_form', $data);
            $this->load->view('footer');
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('diagnosa'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
		'diagnosa' => $this->input->post('diagnosa',TRUE),
	    );

            $this->Diagnosa_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('diagnosa'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Diagnosa_model->get_by_id($id);

        if ($row) {
            $this->Diagnosa_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('diagnosa'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('diagnosa'));
        }
    }

    public function _rules() 
    {
	$this->form_validation->set_rules('diagnosa', 'diagnosa', 'trim|required');

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Diagnosa.php */
/* Location: ./application/controllers/Diagnosa.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2020-04-16 17:18:50 */
/* http://harviacode.com */